---
summary: "Package metadata aligned to ecosystem standard: author, funding, scripts, install badges, FUNDING.yml"
breaking: false
security: false
---

# 0.1.3 — 2026-05-26

## Added

- **`.github/FUNDING.yml`** — GitHub Sponsors and Buy Me a Coffee funding config (`github: cyanheads`, `buy_me_a_coffee: cyanheads`).
- **Install badges** — Claude Desktop (.mcpb download), Cursor (deeplink), VS Code (deeplink) added to README header.
- **`author` field** in `package.json` (`cyanheads <casey@caseyjhand.com>`).
- **`funding` array** in `package.json` (GitHub Sponsors + Buy Me a Coffee).
- **`bugs` and `homepage` fields** in `package.json`.
- **Docker badge** and **MCP SDK badge** added to README badge row.
- **npm badge** added to README badge row.

## Changed

- **Scripts** in `package.json` migrated from `tsx` to `bun run scripts/*.ts`; `start:stdio` and `start:http` now use `bun ./dist/index.js`.
- **`tsx`** removed from `devDependencies` (no longer needed).
- **`packageManager`** field added to `package.json` (`bun@1.3.2`).
- **CLAUDE.md commands table** updated to reflect `bun run` invocations.
